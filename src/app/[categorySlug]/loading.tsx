import { CategorySkeleton } from "@/components/sites/menzu-lol-f7ae197a/shared/StorefrontSkeleton";

/** What a category shelf shows under the header until its cards arrive. */
export default function Loading() {
  return <CategorySkeleton />;
}
