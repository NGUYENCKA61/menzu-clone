import { ProductSkeleton } from "@/components/sites/menzu-lol-f7ae197a/shared/StorefrontSkeleton";

/** What a product page shows under the header until the product arrives. */
export default function Loading() {
  return <ProductSkeleton />;
}
